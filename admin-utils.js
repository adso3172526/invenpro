// admin-utils.js - Thin shim (delegates to src/invenpro/ui/ modules)
// Backward compat: all window globals already set by individual modules above
const { useState: useStateA, useMemo: useMemoA } = React;
